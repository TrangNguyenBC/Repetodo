package com.sleepingworm.repetodo.mainlist

import org.junit.Test

class MainListViewModelTest {
    @Test
    fun addNewTask_fromEmptyList() {

        // Given a fresh MainListViewModel
        // val mainListViewModel = MainListViewModel(taskListDao, ApplicationProvider.getApplicationContext())

        // When adding a new task


        // Then the list there is only 1 task
    }
}